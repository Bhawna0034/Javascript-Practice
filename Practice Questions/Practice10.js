let heading = document.querySelector("h2");
console.dir(heading.innerText);

heading.innerText = heading.innerText + " from ApnaCollege"; //concatenation

let divs = document.querySelectorAll(".box");
console.dir(divs);
